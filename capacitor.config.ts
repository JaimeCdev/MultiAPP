import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'test.test.mikehila',
  appName: 'PruebasCJG',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
